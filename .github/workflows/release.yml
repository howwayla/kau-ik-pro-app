name: Release Desktop App

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        # `target` is the Rust triple; the bun compile target is derived from it
        # by scripts/build-desktop.mjs (single source of truth — see TARGETS).
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
            args: '--target aarch64-apple-darwin'
          - platform: macos-latest
            target: x86_64-apple-darwin
            args: '--target x86_64-apple-darwin'
          - platform: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            args: ''
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: oven-sh/setup-bun@v2

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev \
            librsvg2-dev patchelf libgtk-3-dev

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      # Compile the bundled Node server into a single-file sidecar binary.
      # Same script users run locally (`pnpm desktop:build`), so the
      # triple -> bun-target -> filename logic lives in exactly one place.
      # matrix.target is a trusted, workflow-defined Rust triple (no injection).
      - name: Build nova-server sidecar
        run: node scripts/build-desktop.mjs --sidecar-only --target ${{ matrix.target }}

      # Tauri treats a present-but-empty APPLE_ID as "please notarize", so the
      # notarization env is only injected when real credentials exist.
      - name: Enable notarization (when credentials are configured)
        if: startsWith(matrix.platform, 'macos')
        shell: bash
        run: |
          if [ -n "${{ secrets.APPLE_ID }}" ] && [ -n "${{ secrets.APPLE_PASSWORD }}" ]; then
            {
              echo "APPLE_ID=${{ secrets.APPLE_ID }}"
              echo "APPLE_PASSWORD=${{ secrets.APPLE_PASSWORD }}"
              echo "APPLE_TEAM_ID=${{ secrets.APPLE_TEAM_ID }}"
            } >> "$GITHUB_ENV"
            echo "Notarization enabled"
          else
            echo "Notarization credentials not set — signing only"
          fi

      - name: Build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # macOS code-signing (notarization env is injected by the step
          # above only when credentials are configured)
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Nova Pro ${{ github.ref_name }}'
          releaseBody: |
            Nova Pro 桌面版 — 內建 Node server（sidecar，開啟即自動啟動）、
            多券商交易（富邦／台新／玉山）、系統匣（menu bar）。
            下載對應平台的安裝檔即可使用。

            macOS: .dmg ｜ Windows: .msi / .exe ｜ Linux: .AppImage / .deb / .rpm
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
