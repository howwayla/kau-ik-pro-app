name: Test

on:
  push:
  pull_request:

jobs:
  test:
    # Pure unit tests (target table, drift guard, preflight version + PATH probes).
    # No Rust/bun/install needed — runs in seconds. The matrix proves the
    # cross-platform PATH detection (`commandExists`) actually works per-OS.
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
